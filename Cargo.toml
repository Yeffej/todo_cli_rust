[package]
name = "to-do-list-cli"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
anyhow = "1.0"

[dependencies.uuid]
version = "1.20.0"
# Lets you generate random UUIDs
features = [
    "v4",
]